<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.iu.main.student.StudentDAO">
  
  	<select id="getList" resultType="com.iu.main.student.StudentDTO">
  		SELECT STUDNUM, STUDNAME, STUDAVG FROM STUDENTS ORDER BY STUDNUM DESC
  	</select>
  	
  	<select id="getDetail" resultType="com.iu.main.student.StudentDTO" parameterType="com.iu.main.student.StudentDTO">
  		SELECT STUDNUM, STUDNAME, STUDKOR, STUDENG, STUDMATH, STUDTOTAL, STUDAVG FROM STUDENTS WHERE STUDNUM=#{studNum}
  	</select>
  	
  	<delete id="setDelete" parameterType="com.iu.main.student.StudentDTO">
		DELETE STUDENTS WHERE STUDNUM=#{studNum}
  	</delete>
  	
  	<update id="setUpdate" parameterType="com.iu.main.student.StudentDTO">
  		UPDATE STUDENTS SET
		STUDNUM=#{studNum},
		STUDNAME=#{studName},
		STUDKOR=#{studKor},
		STUDENG=#{studEng},
		STUDMATH=#{studMath},
		STUDTOTAL=#{studTotal},
		STUDAVG=#{studAvg}
		WHERE STUDNUM=#{studNum}
  	</update>
  	
  	<insert id="setAdd" parameterType="com.iu.main.student.StudentDTO">
  		INSERT INTO STUDENTS
		VALUES (STUDENT_SEQ.NEXTVAL, #{studName}, #{studKor}, #{studEng}, #{studMath}, #{studTotal}, #{studAvg})
  	</insert>

  </mapper>